# IoTAirMonitoringDevice
Arduino firmware and AWS cloud code from the UFAR × TUMO Labs IoT project. This repository includes multi-sensor device firmware and the AWS IoT → Lambda → S3 pipeline used for processing and storing air-quality data collected by the hardware system.
